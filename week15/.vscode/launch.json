{
    "configurations": [
        {
            "name": "Launch Program",
            "program": "${workspaceFolder}/parserTest.js",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "pwa-node"
        }
    ]
}